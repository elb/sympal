all:
  sympal_config:
    content_types:
      product:
        model:    Product
        label:    Product
        rendering_options:
          default:
            template: product/view
          other_view:
            template: product/other_view
          custom_action:
            action:   product/customAction
