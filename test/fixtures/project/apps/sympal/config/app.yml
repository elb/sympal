all:
  sympal_config:
    content_types:
      product:
        model:    Product
        label:    Product
        rendering_methods:
          default:
            label:    Default product view
            template: product/view
          other_view:
            template: product/other_view
          custom_action:
            module:   product
            action:   customAction
