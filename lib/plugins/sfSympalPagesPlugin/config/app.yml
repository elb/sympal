all:
  sympal_config:
    content_types:
      page:
        model:    sfSympalPage
        rendering_methods:
          default:
            template: sympal_page/view

    internationalized_models:
      sfSympalPage: [title, body]

  sympal_page:
    route_prefix:  /admin/pages